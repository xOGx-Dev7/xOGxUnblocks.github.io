// Preload script for the Electron wrapper. Keep minimal and secure.
// No Node access exposed to the renderer.

window.addEventListener('DOMContentLoaded', () => {
  // placeholder for future safe IPC or utilities
});
